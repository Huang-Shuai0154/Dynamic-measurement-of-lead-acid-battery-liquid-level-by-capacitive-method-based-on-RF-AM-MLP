# Dynamic-measurement-of-lead-acid-battery-liquid-level-by-capacitive-method-based-on-RF-AM-MLP
In order to eliminate the dynamic measurement error caused by the residual liquid film formation, an integrated learning algorithm combining random forest (RF), multi-layer perceptron (MLP) and attention mechanism (AM) is proposed.
There are seveal scripts in this repository in the "code" folder.The "Final_fft_RF-AM-MLP", "Final_fft_MLP",  "Final_fft_LSTM" are the Grid search of hyperparameters for each model. The"fft RF-AM-MLP.py","fft-MLP.py","fft-LSTM.py" and "Linear static.py" are the training and testing procedures of "RF-AM-MLP","MLP", "static linear model" and "LSTM" respectively. And after training all the three models are saved to remain there parameters unchanged.The "fft_LSTM_plot figures.py" compares the three models using different validation sets which are 30rpm,60rpm,100rpm and variable rates.And different figures are drawn to show the prediction results and error distribution of the three models. All the data appeared in the four scripts are provided accordingly in the "data" folder. (Attention:For the zoom picture of each rate in "fft plot figures.py",readers should change the sample range manually in the code to match the sample range of pictures in the papper.The sample time is 140ms and you can calculate the time according to the number of sample points.)
